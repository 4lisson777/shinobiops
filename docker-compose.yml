services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: web-runner
    env_file:
      - apps/web/.env
    environment:
      DATABASE_URL: "file:./prisma/data/vectorops.db"
      # Production seed — runs once on first deploy when SEED_ADMIN_EMAIL is set.
      # Remove or leave unset after the initial deployment.
      # SEED_ADMIN_NAME: "Your Name"
      # SEED_ADMIN_EMAIL: "admin@yourcompany.com"
      # SEED_ADMIN_PASSWORD: "ChangeMe123!"
    ports:
      - "3005:3000"
    volumes:
      - vectorops-data:/app/apps/web/prisma/data
    init: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 512m

  docs:
    build:
      context: .
      dockerfile: Dockerfile
      target: docs-runner
    ports:
      - "3006:3000"
    init: true
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 256m

volumes:
  vectorops-data:
