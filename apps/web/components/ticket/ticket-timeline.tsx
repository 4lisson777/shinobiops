// TODO: implement TicketTimeline
// Immutable chronological event feed for a ticket/bug:
//   Each event: icon, human-readable label (e.g. "Status changed to In Progress"),
//   actor avatar + name, exact date and time
// Events sourced from GET /api/tickets/[id]/events

export function TicketTimeline() {
  return null
}
