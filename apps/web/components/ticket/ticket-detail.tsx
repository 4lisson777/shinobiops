// TODO: implement TicketDetail
// Full detail view for a ticket or bug:
//   - TicketHeader (ID, title, badges, opener, assignee, dates)
//   - Original fields section (description; bug-only: module, steps, expected/actual, env, customer ID)
//   - "Copy to ClickUp format" button (bugs only)
//   - TicketTimeline (immutable event feed)
//   - Shareable link display

export function TicketDetail() {
  return null
}
